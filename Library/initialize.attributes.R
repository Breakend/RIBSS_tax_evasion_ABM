
#Initialize other attributes of the population

initialize.attributes <- function(population.data)
{
  N <- nrow(population.data)
  #All attributes that are to be set to TRUE
  
 
  return(population.data) 
}  